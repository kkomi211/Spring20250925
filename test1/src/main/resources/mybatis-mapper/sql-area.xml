<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.test1.mapper.AreaMapper">
	<select id="selectArea" parameterType="hashmap" resultType="com.example.test1.model.Area">
		SELECT *
		FROM AREA
		WHERE GU IS NOT NULL 
			AND DONG IS NOT NULL
			<if test="si != ''">
				AND SI = #{si}
			</if>
			<if test="gu != ''">
				AND GU = #{gu}
			</if>
			<if test="dong !=''">
				AND DONG = #{dong}
			</if>
		
		OFFSET #{page} ROWS FETCH NEXT #{pageSize} ROWS ONLY
	</select>
	
	<select id="offset" parameterType="hashmap" resultType="int">
		SELECT COUNT(*)
		FROM AREA
		WHERE GU IS NOT NULL AND DONG IS NOT NULL
		<if test="si != ''">
			AND SI = #{si}
		</if>
		<if test="gu != ''">
			AND GU = #{gu}
		</if>
		<if test="dong != ''">
			AND DONG = #{dong}
		</if>
	</select>
	
	<select id="selectSiList" parameterType="hashmap" resultType="com.example.test1.model.Area">
		SELECT *
		FROM AREA
		WHERE GU IS NULL
	</select>
	
	<select id="selectGuList" parameterType="hashmap" resultType="com.example.test1.model.Area">
		SELECT GU
		FROM AREA
		WHERE SI = #{si} AND GU IS NOT NULL
		GROUP BY GU
	</select>
	
	
	<select id="selectDongList" parameterType="hashmap" resultType="com.example.test1.model.Area">
		SELECT DONG
		FROM AREA
		WHERE 
		    SI = #{si} 
		    AND GU = #{gu} 
		    AND DONG IS NOT NULL
	</select>
	
	
	
</mapper>




