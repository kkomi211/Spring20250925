<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.test1.mapper.ProductMapper">
	<select id="selectProList" parameterType="hashmap" resultType="com.example.test1.model.Product">
		SELECT F.*, I.*, TO_CHAR(PRICE, 'FM999,999,999,999') AS VAL 
		FROM TBL_FOOD F
		INNER JOIN TBL_FOOD_IMG I ON F.FOOD_NO = I.FOOD_NO
		WHERE 1=1
			<if test="search != ''">
			AND (FOOD_NAME LIKE '%' || #{search} || '%' 
			OR FOOD_INFO LIKE '%' || #{search} || '%')
			</if>
			<if test="kind != ''">
			AND (MENU_PART = #{kind}
			OR MENU_NO = #{kind})
			</if>
			<if test="foodNo != null and foodNo != ''">
			AND F.FOOD_NO = #{foodNo}
			</if>
	</select>
	
	<select id="selectMenuList" parameterType="hashmap" resultType="com.example.test1.model.Menu">
		SELECT M.*, NVL(CNT, 0) AS CNT
		FROM TBL_MENU M
		LEFT JOIN (
		    SELECT MENU_PART, COUNT(MENU_PART) AS CNT
		    FROM TBL_MENU
		    GROUP BY MENU_PART
		) T ON M.MENU_NO = T.MENU_PART
		WHERE 1=1
		<if test="depth != '' and depth != null">
			AND DEPTH = #{depth}
		</if>
	</select>
	
	<insert id="insertProduct" parameterType="hashmap" useGeneratedKeys="true" keyColumn="FOOD_NO" keyProperty="foodNo">
		INSERT INTO TBL_FOOD
		VALUES (
			FOOD_SEQ.NEXTVAL, #{foodName}, #{price}, 
			<if test="menuPart == 10">'한식',</if>
			<if test="menuPart == 20">'중식',</if>
			<if test="menuPart == 30">'양식',</if>
			<if test="menuPart == 40">'디저트',</if>
			<if test="menuPart == 50">'음료',</if>
			
			#{foodInfo}, 'Y', #{menuNo}, #{menuPart}
		)
	</insert>
	
	<insert id="insertFoodImg" parameterType="hashmap">
		INSERT INTO TBL_FOOD_IMG
		VALUES ( FOODFILE_SEQ.NEXTVAL, #{foodNo}, #{path}, #{fileName}, #{orgName}, #{size}, #{ext}, 'Y')
	</insert>
	
	<insert id="insertMenu" parameterType="hashmap">
		INSERT INTO TBL_MENU
		VALUES ( #{menuNo}, #{foodName}, #{menuPart}, '2')
	</insert>
	
	<insert id="insertPaymentHistory" parameterType="hashmap">
		INSERT INTO PAYMENT_HISTORY
		VALUES( #{orderId}, #{userId}, #{amount}, #{productNo}, SYSDATE)
	</insert>
	
	
</mapper>